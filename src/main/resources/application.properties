# AWS Configuration
aws.region=ap-southeast-1
aws.s3.bucket=zzhe-sin-encrption-client-lab-bucket-a9733f6

# AWS KMS Configuration
# For standard KMS key (created by Pulumi):
# aws.kms.keyId=${KMS_KEY_ID}

# For imported key with EXTERNAL origin:
# After running the import_key_material.sh script on the EC2 instance,
# replace the above line with the NEW_KMS_KEY_ID from /home/ec2-user/kms_env.sh
# Example: aws.kms.keyId=12345678-abcd-1234-efgh-123456789012

# IMPORTANT: Use the key with EXTERNAL origin for proper KMS integration
aws.kms.keyId=${KMS_KEY_ID}
